# FastAPI and Server / FastAPI మరియు సర్వర్
fastapi==0.104.1
uvicorn[standard]==0.24.0

# Database / డేటాబేస్
sqlalchemy==2.0.23
psycopg2-binary==2.9.9  # For PostgreSQL / PostgreSQL కోసం
alembic==1.12.1  # Database migrations / డేటాబేస్ migrations

# Validation / Validation
pydantic==2.5.0
pydantic-settings==2.1.0
pydantic[email]==2.5.0

# Security / భద్రత
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
python-multipart==0.0.6

# Testing / టెస్టింగ్
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Utilities / యుటిలిటీస్
python-dotenv==1.0.0